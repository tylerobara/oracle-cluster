traefik:
  image:
    registry: ghcr.io
    repository: 11notes/traefik
    tag: 3.7.5@sha256:87bbbf6eba10b381f1cd151e0a27ac309dc6b26c80b25c5e664ccf9a842be69c

  ingressRoute:
    dashboard:
      enabled: true
      matchRule: Host(`traefik.cloud.obara.xyz`)
      entryPoints:
        - websecure
      middlewares:
        - name: traefik-oidc-auth

  global:
    sendAnonymousUsage: false
    checkNewVersion: false

  additionalArguments:
    - "--providers.kubernetesingress.ingressendpoint.publishedservice=traefik/traefik"
    - "--serversTransport.insecureSkipVerify=true"
    - "--log.level=INFO"
    - "--providers.kubernetescrd.allowexternalnameservices=true"
    - "--providers.kubernetescrd.allowCrossNamespace=true"
  providers:
    kubernetesCRD:
      enabled: true
      allowExternalNameServices: true
    kubernetesIngress:
      enabled: true
      allowExternalNameServices: true
      publishedService:
        enabled: false

  ports:
    web:
      http:
        redirections:
          entryPoint:
            to: websecure
            scheme: https
            permanent: true
    forgejo-ssh:
      port: 2222
      expose:
        default: true
    websecure:
      http:
        transport:
          respondingTimeouts:
            readTimeout: 180s

  tlsStore:
    default:
      defaultCertificate:
        secretName: cloud-obara-xyz-tls

  log:
    level: "DEBUG"
  accessLog:
    enabled: true
    filters:
      statusCodes: "400-599"

  gateway:
    enabled: false

  experimental:
    plugins:
      traefik-oidc-auth:
        moduleName: "github.com/sevensolutions/traefik-oidc-auth"
        version: "v0.20.1"

  service:
    loadBalancerIP: "10.0.0.68"
